{ config, lib, pkgs, ... }:

{
  # TODO: Configure Arc browser
  # pkgs.chromium has no support for darwin

  # programs.chromium = {
  #   enable = true;
  #   extensions = [
  #     { id = "cjpalhdlnbpafiamejdnhcphjbkeiagm"; }
  #   ];
    
  #   package = pkgs.chromium;
  # };
}
